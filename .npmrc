# Don't re-run an install check before every `pnpm run` script. It escalates the
# harmless "ignored build scripts" notice (esbuild ships prebuilt binaries via
# optional deps, so its postinstall isn't needed) into a hard error.
verify-deps-before-run=false
